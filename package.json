{
  "name": "queuectl",
  "version": "0.1.0",
  "description": "CLI-based background job queue using filesystem JSON persistence.",
  "bin": {
    "queuectl": "index.js"
  },
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "worker": "node index.js worker start",
    "dev": "nodemon --watch src --ext js,json --exec \"node index.js\"",
    "queuectl": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "http-status-codes": "^2.3.0",
    "mongoose": "^8.19.3",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
